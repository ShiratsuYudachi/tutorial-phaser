{
  "name": "tutorial-phaser",
  "version": "1.0.0",
  "description": "Colyseus + Phaser Game",
  "scripts": {
    "postinstall": "npm install --prefix client && npm install --prefix server",
    "start": "concurrently \"npm run start --prefix server\" \"npm run start --prefix client\""
  },
  "dependencies": {
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
